{% extends 'base.html.twig' %}
{% block title %}Edit Customer • {{ customer.companyName }}{% endblock %}
{% block body %}
<div class="mx-auto max-w-3xl px-4 py-8 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between">
        <h1 class="text-2xl font-semibold text-gray-900 dark:text-white">Edit Customer</h1>
        <a href="{{ path('customer_show', {id: customer.id}) }}"
            class="text-sm/6 font-semibold text-indigo-600 dark:text-indigo-400">Back</a>
    </div>
    <div class="mt-6 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-gray-900/5 dark:bg-white/5 dark:ring-white/10">
        {% include 'customer/_form.html.twig' with { form: form, button_label: 'Update' } %}
    </div>
</div>
{% endblock %}